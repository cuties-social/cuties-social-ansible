# This starts a simple nginx for the letsencrypt acme challenge
server {
  listen 80;
  listen [::]:80;
  server_name .{{ mastodon_host }}; # Dot added to catch subdomains such as www.
  root {{ mastodon_path }}/public;
  location /.well-known/acme-challenge/ { allow all; }
}
